management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

spring:
  security:
    user:
      name: admin
      password: pwd
      roles:
        - 'ADMIN'
        - 'ACTUATOR'